---
const { eyebrow, title, text, as = "h2" } = Astro.props;
const Heading = as;
---
<div class="section-title">
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <Heading>{title}</Heading>
  {text && <p>{text}</p>}
</div>
